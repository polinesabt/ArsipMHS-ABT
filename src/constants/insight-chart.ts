/**
 * Ukuran standar chart & section dashboard insight agar selaras dan konsisten.
 */
export const CHART_HEIGHT = 280;
export const CHART_HEIGHT_TABBED = 280;
/** Min-height area konten card (loading/empty/chart) supaya card tidak mengembang/mengecil saat ganti tab atau state. */
export const CARD_CONTENT_MIN_HEIGHT = 320;
/** Min-height untuk section bertab (Prestasi, Publikasi, Luaran Riset). */
export const TABBED_SECTION_MIN_HEIGHT = 360;
